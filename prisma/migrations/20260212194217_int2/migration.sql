BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[StockLocation] ADD [address] NVARCHAR(1000),
[contactInfo] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
